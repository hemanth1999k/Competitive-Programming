#include<bits/stdc++.h>
using namespace std;

/
/*
    minimum number in all subsegements of k
    a number should present in all subsegemnt of size k
    1 n numbers only
    
    if only one elem present 
*/

int main(){
    // freopen("input.txt","r",stdin);
    long long int n,t;
    cin>>t;
    while(t--){
        cin>>n;
        int arr[n];
        for(int i=0;i<n;i++)cin>>arr[i];

    }
}

